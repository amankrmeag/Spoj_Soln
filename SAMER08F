#include<bits/stdc++.h>
using namespace std;
int cal(int n)
{
    int s=0;
    for (int i=1;i<=n;i++)
    s+=(i*i);
        return s;
}
int main()
{
    int n;
    while (1)
    {
        cin>>n;
        if (n==0)
            break;
        cout<<cal(n)<<endl; 
    }   
        return 0;
}