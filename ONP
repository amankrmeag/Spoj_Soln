#include<iostream>
#include<stack>
#include<string>
using namespace std;
string op="(-+*/^)";
int find(char x)
{
    for (int i=0;i<7;i++)
    if (op[i]==x)
    return i;
        return -1;
}
int main(void)
{
    int t;
    cin>>t;
                while (t--)
    {
        string str,ans;
        cin>>str;
        stack <char> S;
            int j=0,fla=0,found=0;
        for (int i=0;i<str.length();i++)
        {
        