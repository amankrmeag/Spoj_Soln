#include<iostream>
#include<cmath>
using namespace std;
int main(void)
{
        long int t;
          int sum , i,j;
          long long int n;
          cin>>t;
          while(t--)
     {
        sum=0;
        cin>>n;
        for (i=1;pow(5,i)<=n;i++)
        {
            sum+=n/pow(5,i);
                     }
        cout<<sum<<endl;
     }
        return 0;
}