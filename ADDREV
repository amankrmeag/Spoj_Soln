def revers(x):
    a=0
    while (x!=0):
        a=a*10+x%10
        x=x//10
    return a
z=int(input())
for i in range (0,z):
    a,b=map(int,input().split())
    a=revers(a)+revers(b)
    print(revers(a))